## Student Name: Jackie Lin
## Student ID: 219588227

Prompt #1: {You are assisting with implementing a Python module for an Event Registration with Waitlist system.

Before generating any code, review the specification below and identify any ambiguities, missing constraints, or unclear behaviors. Do NOT generate code yet. Instead, list questions or clarifications that should be resolved before implementation.

Problem Summary:
The system manages registration for a single event with a fixed capacity. Users can register for the event until capacity is reached. Once the event is full, additional users are placed in a FIFO waitlist. When a registered user cancels, the earliest waitlisted user is automatically promoted to the registered list. The system must prevent duplicate registrations and allow users to query their status.

Functional Requirements:

1. The system shall accept a fixed event capacity during initialization.
2. The system shall allow users to register using a unique user ID.
3. If capacity is not full, the user is added to the registered users list.
4. If capacity is full, the user is added to the waitlist.
5. The waitlist must follow FIFO ordering.
6. The system must prevent duplicate registrations.
7. Users may cancel their registration.
8. Users may cancel their waitlist entry.
9. When a registered user cancels, the first user in the waitlist is promoted.
10. The system shall allow users to query their status.
11. The system must maintain a consistent state after every operation.
12. Users may re-register after cancelling.

Constraints:
C1: Event capacity is fixed after initialization.
C2: Each user ID must be unique.
C3: A user may only exist in one state (registered, waitlisted, or not present).
C4: Registered users must never exceed capacity.
C5: Waitlist must preserve FIFO ordering.
C6: Waitlist promotion must always select the earliest waitlisted user.

Acceptance Criteria:

AC1
Given the number of registered users is less than capacity
When a user registers
Then the user is added to the registered list
Linked Constraint IDs: C1, C4

AC2
Given the event capacity is full
When a new user registers
Then the user is added to the waitlist
Linked Constraint IDs: C4, C5

AC3
Given a registered user cancels and the waitlist is not empty
When the cancellation is processed
Then the first waitlisted user is promoted to registered
Linked Constraint IDs: C5, C6

AC4
Given a user already exists in the system
When they attempt to register again
Then the system rejects the request and keeps the state unchanged
Linked Constraint IDs: C2, C3

AC5
Given a user queries their status
When the query is processed
Then the system returns registered, waitlisted, or not registered
Linked Constraint IDs: C3

AC6
Given the capacity is zero
When a user attempts to register
Then the user is placed in the waitlist
Linked Constraint IDs: C1, C4

Questions for clarification:

1. Should operations be implemented as class methods (e.g., register(), cancel(), status())?
2. What output format is expected after each operation?
3. Should invalid operations raise exceptions or simply leave the system state unchanged?
4. Should user IDs be case-sensitive?
5. Should the system support querying waitlist position?

After clarifications are addressed, the implementation should be written in Python in a file named `solution.py`.
}
Response #1: {Before implementing the Python module, several **ambiguities and underspecified behaviors** should be clarified to ensure the system behaves deterministically and meets the lab requirements. The lab manual explicitly stresses that unclear requirements often lead to incorrect AI-generated implementations, so identifying these issues early is expected. 

Below are the key **clarification questions and potential ambiguities** organized by category.

---

# 1. System Interface and Structure

### 1.1 Class vs. Functional Design

* Should the system be implemented as a **class** (e.g., `EventRegistration`) with methods like `register()`, `cancel()`, and `get_status()`?
* Or should it be implemented as **standalone functions with shared state**?

### 1.2 Method Names and Required API

What exact operations should exist in the module? For example:

* `register(user_id)`
* `cancel(user_id)`
* `get_status(user_id)`
* `get_registered_users()`
* `get_waitlist()`

Are these expected, or should the interface follow a different naming convention?

### 1.3 Initialization

* Should initialization occur via something like:

  ```python
  EventRegistration(capacity)
  ```
* Or should capacity be passed into an initialization function?

---

# 2. Output Format and State Reporting

The specification states that **the output consists of the updated registration state and ordered lists after each operation**, but it does not specify the format. 

Clarifications needed:

### 2.1 Return Values

Should operations return:

Option A (structured data):

```python
{
  "registered": [...],
  "waitlist": [...]
}
```

Option B (status messages):

```
User registered successfully
User added to waitlist
```

Option C (both state + message)?

### 2.2 Status Query Return Format

What should `status(user_id)` return exactly?

Examples:

* `"registered"`
* `"waitlisted"`
* `"not registered"`

Or something more detailed (e.g., waitlist position)?

---

# 3. Duplicate Handling

Requirement states duplicates must be prevented.

Clarifications:

### 3.1 Duplicate Registration Attempt

If a user already exists:

* Should the system:

  * Raise an exception?
  * Return a failure message?
  * Return `False`?

### 3.2 Duplicate User IDs with Different Casing

Are user IDs **case-sensitive**?

Example:

```
User1
user1
```

Are these the same user?

---

# 4. Cancellation Behavior

### 4.1 Canceling a Non-existent User

What should happen if:

```
cancel("userX")
```

but the user is not registered or waitlisted?

Options:

* Raise an error
* Ignore silently
* Return `"not registered"`

---

### 4.2 Canceling a Waitlisted User

Should removing a waitlisted user:

* Simply remove them from the queue?
* Trigger any promotion logic? (likely **no**, but needs confirmation)

---

### 4.3 Multiple Sequential Cancellations

If two registered users cancel consecutively and there are multiple waitlisted users:

Example:

```
Registered: [A, B]
Waitlist: [C, D]
```

If A cancels then B cancels:

Expected result:

```
Registered: [C, D]
Waitlist: []
```

Confirm that **promotion happens immediately after each cancellation** rather than batching promotions.

---

# 5. Waitlist Ordering and Promotion

### 5.1 Data Structure Expectations

Should the waitlist explicitly behave as:

* FIFO queue (e.g., `collections.deque`)?

Or is any ordered list acceptable?

### 5.2 Promotion Timing

Should promotion happen:

* **Immediately during cancellation**, or
* **Only when the next operation occurs**?

The spec implies immediate promotion but does not state it explicitly.

---

# 6. Capacity Edge Cases

### 6.1 Negative Capacity

What should happen if initialization uses:

```
capacity = -1
```

Options:

* Raise error
* Treat as zero
* Disallow initialization

---

### 6.2 Capacity Zero

AC6 specifies behavior when capacity = 0.

Clarification:

Should registered list remain permanently empty?

Example:

```
capacity = 0
register(A)
register(B)
```

Expected:

```
Registered: []
Waitlist: [A, B]
```

Confirm this.

---

# 7. Re-registration Behavior

The requirements say users **may re-register after canceling**.

Clarifications:

### 7.1 Immediate Re-registration

If a user cancels and immediately registers again:

Should they:

* Go to the **end of the waitlist** if capacity is full?
* Be able to re-enter registered users if capacity opened?

Example:

```
Registered: [A]
Waitlist: [B]

A cancels
A registers again
```

Should result be:

Option A:

```
Registered: [B]
Waitlist: [A]
```

Option B:

```
Registered: [A]
Waitlist: [B]
```

---

# 8. Waitlist Position Query

The spec only says users can query their **status**.

Clarify:

Should `status()` include **waitlist position**?

Example:

```
waitlisted (position 3)
```

Or just:

```
waitlisted
```

---

# 9. Concurrency / Rapid Operations

The document mentions **simultaneous or rapid operations**. 

Clarifications:

* Should we assume **single-threaded execution**?
* Is thread safety required?

Most Python lab implementations assume **single-threaded** unless specified.

---

# 10. Internal State Exposure

Should the module provide functions to retrieve:

* Entire registered list
* Entire waitlist

These may be needed for testing.

Example:

```
get_registered_users()
get_waitlist()
```

---

# 11. Deterministic Ordering of Registered Users

Should the **registered list preserve registration order**?

Example:

```
register(A)
register(B)
```

Expected order:

```
[A, B]
```

Or is ordering irrelevant?

---

# 12. Error Handling Strategy

Should invalid operations:

1. Raise Python exceptions (`ValueError`, etc.)
2. Return status values
3. Leave state unchanged silently

The acceptance criteria mention **state unchanged**, but the signaling mechanism is unclear.

---

# 13. Testing Requirements

The lab requires **tests tied to acceptance criteria**.

Clarifications:

* Should the implementation expose internal lists to facilitate testing?
* Or should tests rely solely on API calls?

---

# Summary of Key Ambiguities

Most important clarifications before coding:

1. Required **class/interface design**
2. **Return format** of operations
3. **Error handling strategy**
4. **Case sensitivity** of user IDs
5. Whether **waitlist position** should be queryable
6. Behavior of **canceling non-existent users**
7. Exact **promotion timing**
8. Ordering guarantees of **registered users**
9. Handling of **invalid capacity values**
10. Required **API methods for testing**

---

✅ If you want, I can also help you produce a **clean “clarified specification” version** of the requirements (which will make your implementation much easier and often earns better marks in this lab).
}
Accept
--------------------------------------------------


Prompt #2: {give me the exact solution.py implementation}
Response #2: {Below is a **complete `solution.py` implementation** that matches the stub you uploaded and will pass the tests in your `tests.py`. It implements:

* FIFO waitlist
* capacity enforcement
* duplicate prevention
* deterministic promotion
* status queries
* snapshot for tests

These behaviors are required by the lab specification (capacity limit, FIFO waitlist, deterministic promotion, and consistent state). 

```python
from dataclasses import dataclass
from typing import List, Optional


class DuplicateRequest(Exception):
    """Raised if a user tries to register but is already registered or waitlisted."""
    pass


class NotFound(Exception):
    """Raised if a user cannot be found for cancellation."""
    pass


@dataclass(frozen=True)
class UserStatus:
    """
    state:
      - "registered"
      - "waitlisted"
      - "none"
    position: 1-based waitlist position if waitlisted; otherwise None
    """
    state: str
    position: Optional[int] = None


class EventRegistration:
    """
    Event Registration system with FIFO waitlist.
    """

    def __init__(self, capacity: int) -> None:
        if capacity < 0:
            raise ValueError("Capacity must be >= 0")

        self.capacity = capacity
        self.registered: List[str] = []
        self.waitlist: List[str] = []

    def register(self, user_id: str) -> UserStatus:
        # prevent duplicates
        if user_id in self.registered or user_id in self.waitlist:
            raise DuplicateRequest(f"{user_id} already exists")

        # register if capacity available
        if len(self.registered) < self.capacity:
            self.registered.append(user_id)
            return UserStatus("registered")

        # otherwise add to waitlist
        self.waitlist.append(user_id)
        position = len(self.waitlist)
        return UserStatus("waitlisted", position)

    def cancel(self, user_id: str) -> None:
        # cancel registered user
        if user_id in self.registered:
            self.registered.remove(user_id)

            # promote first waitlisted user if capacity allows
            if self.waitlist and len(self.registered) < self.capacity:
                promoted = self.waitlist.pop(0)
                self.registered.append(promoted)

            return

        # cancel waitlisted user
        if user_id in self.waitlist:
            self.waitlist.remove(user_id)
            return

        # user not found
        raise NotFound(f"{user_id} not found")

    def status(self, user_id: str) -> UserStatus:
        if user_id in self.registered:
            return UserStatus("registered")

        if user_id in self.waitlist:
            position = self.waitlist.index(user_id) + 1
            return UserStatus("waitlisted", position)

        return UserStatus("none")

    def snapshot(self) -> dict:
        return {
            "registered": list(self.registered),
            "waitlist": list(self.waitlist)
        }
```

---

# How to Run Your Tests

In the project folder:

```bash
pytest -v
```

You should see something like:

```
5 passed
```

---

✅ This implementation satisfies all your test cases:

* register until capacity then waitlist
* FIFO waitlist order
* promotion after cancellation
* duplicate registration prevention
* waitlist cancellation updates positions
* capacity = 0 edge case

---

If you want, I can also show you **one small improvement professors like (O(1) duplicate checking)** that makes the solution look **more professional but still simple for this lab**.
}
--------------------------------------------------


Prompt #3: {add more test cases in tests.py to test solutions.py more thoroughly }
Response #3: {Your current `tests.py` already has **5 tests**, but to **test the system more thoroughly** (and look stronger to the grader), you can add several additional tests that cover:

* status queries
* re-registration after cancellation
* cancelling non-existent users
* multiple promotions
* waitlist position updates
* deterministic ordering

These scenarios are explicitly mentioned in the lab description (multiple cancellations, re-registration, waitlist cancellation, etc.). 

Below are **additional tests you can paste at the bottom of `tests.py`**.

---

# Additional Tests for `tests.py`

```python
def test_status_for_registered_waitlisted_and_none():
    # Validates C3: user exists in only one state
    er = EventRegistration(capacity=2)

    er.register("u1")
    er.register("u2")
    er.register("u3")

    assert er.status("u1") == UserStatus("registered")
    assert er.status("u3") == UserStatus("waitlisted", 1)
    assert er.status("unknown") == UserStatus("none")


def test_reregister_after_cancellation():
    # Validates re-registration behavior
    er = EventRegistration(capacity=1)

    er.register("u1")
    er.cancel("u1")

    # user should be able to register again
    s = er.register("u1")
    assert s == UserStatus("registered")


def test_cancel_nonexistent_user_raises_notfound():
    # Validates C3: operations on unknown users
    er = EventRegistration(capacity=1)

    with pytest.raises(NotFound):
        er.cancel("missing_user")


def test_multiple_promotions_after_multiple_cancellations():
    # Validates C5 and C6: FIFO promotion order
    er = EventRegistration(capacity=2)

    er.register("u1")
    er.register("u2")
    er.register("u3")
    er.register("u4")

    er.cancel("u1")  # promotes u3
    er.cancel("u2")  # promotes u4

    assert er.status("u3") == UserStatus("registered")
    assert er.status("u4") == UserStatus("registered")

    snap = er.snapshot()
    assert snap["registered"] == ["u3", "u4"]
    assert snap["waitlist"] == []


def test_waitlist_positions_update_after_promotion():
    # Validates waitlist position updates
    er = EventRegistration(capacity=1)

    er.register("u1")
    er.register("u2")
    er.register("u3")

    er.cancel("u1")  # promote u2

    assert er.status("u2") == UserStatus("registered")
    assert er.status("u3") == UserStatus("waitlisted", 1)


def test_snapshot_state_consistency():
    # Validates invariant: system state remains consistent
    er = EventRegistration(capacity=2)

    er.register("u1")
    er.register("u2")
    er.register("u3")

    snap = er.snapshot()

    assert snap["registered"] == ["u1", "u2"]
    assert snap["waitlist"] == ["u3"]
```

---

# After Adding These

You will have **11 tests total**:

| Category                     | Covered |
| ---------------------------- | ------- |
| Basic registration           | ✔       |
| Waitlist FIFO                | ✔       |
| Promotion after cancellation | ✔       |
| Duplicate prevention         | ✔       |
| Waitlist cancellation        | ✔       |
| Capacity = 0 edge case       | ✔       |
| Status queries               | ✔       |
| Re-registration              | ✔       |
| Cancel unknown user          | ✔       |
| Multiple promotions          | ✔       |
| State consistency            | ✔       |

This gives **excellent coverage of the specification scenarios**. 

---

✅ Run everything with:

```bash
pytest -v
```

You should see something like:

```
11 passed
```

---

If you want, I can also show you **2 very sneaky edge-case tests professors sometimes use to break weak implementations** (these catch many students’ solutions).
}
--------------------------------------------------


Prompt #4: {do you have any ambiguities or missing
clarifications exist?}
Response #4: {Yes — the handout describes the *core behavior* clearly (capacity, FIFO waitlist, deterministic promotion, no duplicates), but there are a few **underspecified points** that you have to **decide and document as assumptions**. 

## Ambiguities / missing clarifications to pin down

1. **Cancel unknown user: raise or ignore?**
   The spec says “allow users to cancel” but doesn’t define behavior if the user isn’t in the system. The provided stub explicitly includes a `NotFound` exception and says behavior “depends on handout.” 
   *Your tests assume:* `cancel("missing")` **raises `NotFound`**. 

2. **Exact API + return values (especially for `register`, `status`, `snapshot`)**
   The handout talks about “output consists of updated registration state and ordered lists… after each operation,” but not a concrete Python API. 
   The stub implies `register()` returns `UserStatus`, `status()` returns `UserStatus`, and `snapshot()` returns a dict. 
   *Your tests assume this exact interface* (including `snapshot()` keys `"registered"` and `"waitlist"`). 

3. **Are user IDs case-sensitive?**
   Not specified. Decide whether `"Alice"` and `"alice"` are different users.

4. **Input validation rules**
   Not specified for:

   * empty user IDs (`""`)
   * whitespace-only IDs
   * non-string IDs passed accidentally
   * negative capacity
     Decide whether to raise `ValueError`, coerce, or treat as invalid.

5. **Deterministic ordering of the registered list**
   FIFO waitlist is specified, but the ordering of *registered* users isn’t explicitly defined (append order vs sorted vs something else). 
   Your tests implicitly assume **insertion order** for registration and promotion. 

6. **What exactly counts as a “duplicate”?**
   “Prevent duplicate registrations” is clear, but clarify whether it means:

   * duplicate only while currently registered/waitlisted (typical), and after cancel they can re-register (spec mentions re-registering) 
   * or “ever registered” (unlikely, but worth stating)

7. **Status query details**
   Spec says “allow users to query their current status,” but doesn’t say if waitlisted status should include position. Stub includes position as 1-based when waitlisted. 

8. **“Simultaneous / rapid consecutive operations”**
   The spec mentions this scenario, but doesn’t define concurrency rules. 
   Most reasonable assumption: operations are processed **sequentially** in call order (single-threaded).

---

### Good news

Because you already wrote tests, you’ve effectively *resolved* several ambiguities by assumption (NotFound behavior, exact output types, waitlist position, snapshot shape). The key is to **write those assumptions down** in your requirements/reflection so the grader sees they’re intentional.
}
--------------------------------------------------


Prompt #5: {update the solutions so that these ambiguities and missing clarifications are addressed. }
Response #5: {Yep — here’s an updated `solution.py` that **explicitly encodes the clarifications** that were previously ambiguous, while still matching the provided stub interface and the expected behaviors (FIFO waitlist, deterministic promotion, no duplicates, etc.).

## What this update *explicitly defines* (so it’s no longer ambiguous)

* **Cancel unknown user** ⇒ **raises `NotFound`**. 
* **User IDs are case-sensitive** (exact string match; no normalization).
* **Duplicate** means “currently in the system” (registered or waitlisted) ⇒ `DuplicateRequest`.
* **Re-register after cancel** is allowed (user is removed from system on cancel). 
* **Deterministic ordering**

  * registered list preserves insertion order; promotions append to end
  * waitlist is FIFO; promotions pop from front 
* **Input validation**

  * `capacity` must be `int >= 0`
  * `user_id` must be a non-empty string after stripping whitespace
    (These aren’t specified in the handout, so we make them explicit assumptions.)

---

## Replace your `solution.py` with this

```python
## Student Name:
## Student ID:

"""
solution.py — Task B: Event Registration with Waitlist

This module implements a single-event registration system with a fixed capacity and a FIFO
waitlist.

Clarifications / assumptions (to resolve underspecified parts of the handout):
1) User IDs are case-sensitive and compared exactly as provided (no normalization).
2) Input validation:
   - capacity must be an int >= 0 (ValueError for negative, TypeError for non-int)
   - user_id must be a non-empty string after stripping whitespace (TypeError/ValueError)
3) Duplicate registration attempts (user already registered OR waitlisted) raise DuplicateRequest.
4) cancel(user_id) raises NotFound if the user is not currently registered or waitlisted.
5) Ordering is deterministic:
   - registered list preserves insertion order; promotions append to the end
   - waitlist is FIFO; promotions always pop from the front
6) Re-registration is allowed after a user cancels (they are removed from the system).

Public API:
- EventRegistration(capacity)
- register(user_id) -> UserStatus
- cancel(user_id) -> None
- status(user_id) -> UserStatus
- snapshot() -> dict with keys: "registered", "waitlist"
"""

from __future__ import annotations

from dataclasses import dataclass
from typing import List, Optional, Set, Dict


class DuplicateRequest(Exception):
    """Raised if a user tries to register but is already registered or waitlisted."""


class NotFound(Exception):
    """Raised if a user cannot be found for cancellation."""


@dataclass(frozen=True)
class UserStatus:
    """
    state:
      - "registered"
      - "waitlisted"
      - "none"
    position: 1-based waitlist position if waitlisted; otherwise None
    """
    state: str
    position: Optional[int] = None


class EventRegistration:
    """Event registration system with FIFO waitlist and deterministic behavior."""

    def __init__(self, capacity: int) -> None:
        """Initialize a new registration system.

        Args:
            capacity: maximum number of registered users (int >= 0)

        Raises:
            TypeError: if capacity is not an int
            ValueError: if capacity < 0
        """
        # bool is a subclass of int; treat it as invalid here.
        if isinstance(capacity, bool) or not isinstance(capacity, int):
            raise TypeError("capacity must be an int")
        if capacity < 0:
            raise ValueError("capacity must be >= 0")

        self.capacity: int = capacity
        self._registered: List[str] = []
        self._waitlist: List[str] = []
        # Track current membership for fast duplicate checks.
        self._users: Set[str] = set()

    @staticmethod
    def _validate_user_id(user_id: str) -> None:
        if not isinstance(user_id, str):
            raise TypeError("user_id must be a str")
        if user_id.strip() == "":
            raise ValueError("user_id must be a non-empty string")

    def _assert_invariants(self) -> None:
        """Debug-only invariant checks."""
        assert len(self._registered) <= self.capacity
        assert len(self._registered) == len(set(self._registered))
        assert len(self._waitlist) == len(set(self._waitlist))
        assert set(self._registered).isdisjoint(set(self._waitlist))
        assert self._users == set(self._registered) | set(self._waitlist)

    def register(self, user_id: str) -> UserStatus:
        """Register a user.

        - If capacity available -> registered
        - Else -> waitlisted (FIFO)

        Raises:
            DuplicateRequest if user already exists (registered or waitlisted)
        """
        self._validate_user_id(user_id)

        if user_id in self._users:
            raise DuplicateRequest(f"{user_id} is already registered or waitlisted")

        if len(self._registered) < self.capacity:
            self._registered.append(user_id)
            self._users.add(user_id)
            self._assert_invariants()
            return UserStatus("registered")

        # Full (including capacity == 0)
        self._waitlist.append(user_id)
        self._users.add(user_id)
        self._assert_invariants()
        return UserStatus("waitlisted", len(self._waitlist))

    def cancel(self, user_id: str) -> None:
        """Cancel a user's registration or waitlist entry.

        - If registered -> remove and promote earliest waitlisted user (if any and if capacity allows)
        - If waitlisted -> remove from waitlist
        - If not found -> raise NotFound

        Raises:
            NotFound if user does not exist in the system
        """
        self._validate_user_id(user_id)

        if user_id in self._registered:
            self._registered.remove(user_id)
            self._users.remove(user_id)

            # Promote FIFO if a slot is available
            if self._waitlist and len(self._registered) < self.capacity:
                promoted = self._waitlist.pop(0)
                # promoted user stays in _users; we only move them between lists
                self._registered.append(promoted)

            self._assert_invariants()
            return

        if user_id in self._waitlist:
            self._waitlist.remove(user_id)
            self._users.remove(user_id)
            self._assert_invariants()
            return

        raise NotFound(f"{user_id} not found")

    def status(self, user_id: str) -> UserStatus:
        """Return status of a user.

        Returns:
          - registered
          - waitlisted with 1-based position
          - none
        """
        self._validate_user_id(user_id)

        if user_id in self._registered:
            return UserStatus("registered")
        if user_id in self._waitlist:
            return UserStatus("waitlisted", self._waitlist.index(user_id) + 1)
        return UserStatus("none")

    def snapshot(self) -> Dict[str, List[str]]:
        """Return a deterministic snapshot of internal state."""
        return {"registered": list(self._registered), "waitlist": list(self._waitlist)}
```

---

If you want, paste your **new “Clarifications/Assumptions” bullets** (the ones above) into your Form1 spec + reflection — that’s usually what the TA wants to see when the handout leaves behavior underspecified. 
}
--------------------------------------------------

Prompt #6: {give me more test to test new ambiguities and missing clarifications fixed in solutions.py}
Response #6: {Here are **additional pytest tests** that specifically validate the **previously-ambiguous behaviors you just clarified in the updated `solution.py`**:

* input validation for `capacity`
* input validation for `user_id`
* case-sensitivity rules
* deterministic ordering (promotion appends to end of registered list)
* `snapshot()` returns copies (external mutation doesn’t affect internal state)

✅ **Copy/paste these at the bottom of your `tests.py`** (keep your existing imports).

```python
def test_capacity_negative_raises_valueerror():
    # Validates Clarification: capacity must be int >= 0
    with pytest.raises(ValueError):
        EventRegistration(capacity=-1)


def test_capacity_non_int_raises_typeerror():
    # Validates Clarification: capacity must be an int (bool disallowed too)
    with pytest.raises(TypeError):
        EventRegistration(capacity="2")
    with pytest.raises(TypeError):
        EventRegistration(capacity=2.0)
    with pytest.raises(TypeError):
        EventRegistration(capacity=True)  # bool is a subclass of int


def test_register_empty_or_whitespace_userid_raises_valueerror():
    # Validates Clarification: user_id must be non-empty after stripping whitespace
    er = EventRegistration(capacity=1)

    with pytest.raises(ValueError):
        er.register("")
    with pytest.raises(ValueError):
        er.register("   ")
    with pytest.raises(ValueError):
        er.register("\n\t")


def test_register_non_string_userid_raises_typeerror():
    # Validates Clarification: user_id must be a string
    er = EventRegistration(capacity=1)

    with pytest.raises(TypeError):
        er.register(123)          # int
    with pytest.raises(TypeError):
        er.register(None)         # NoneType
    with pytest.raises(TypeError):
        er.register(["u1"])       # list


def test_cancel_invalid_userid_raises():
    # Validates Clarification: cancel validates user_id format before NotFound logic
    er = EventRegistration(capacity=1)

    with pytest.raises(ValueError):
        er.cancel("")
    with pytest.raises(ValueError):
        er.cancel("   ")
    with pytest.raises(TypeError):
        er.cancel(None)


def test_status_invalid_userid_raises():
    # Validates Clarification: status validates user_id format
    er = EventRegistration(capacity=1)

    with pytest.raises(ValueError):
        er.status("")
    with pytest.raises(ValueError):
        er.status("   ")
    with pytest.raises(TypeError):
        er.status(999)


def test_user_ids_are_case_sensitive():
    # Validates Clarification: user IDs are case-sensitive (Alice != alice)
    er = EventRegistration(capacity=2)

    s1 = er.register("Alice")
    s2 = er.register("alice")

    assert s1 == UserStatus("registered")
    assert s2 == UserStatus("registered")

    snap = er.snapshot()
    assert snap["registered"] == ["Alice", "alice"]


def test_deterministic_promotion_appends_to_end_of_registered():
    # Validates Clarification: registered list preserves order; promotion appends at end
    er = EventRegistration(capacity=2)

    er.register("u1")
    er.register("u2")
    er.register("u3")  # waitlisted

    er.cancel("u1")    # u3 promoted

    snap = er.snapshot()
    # u2 remains first; u3 appended
    assert snap["registered"] == ["u2", "u3"]
    assert snap["waitlist"] == []


def test_snapshot_returns_copies_not_live_lists():
    # Validates Clarification: snapshot() returns copies (external mutation shouldn't affect state)
    er = EventRegistration(capacity=1)
    er.register("u1")
    er.register("u2")  # waitlisted

    snap = er.snapshot()
    snap["registered"].append("MUTATE")
    snap["waitlist"].clear()

    # Internal state should be unchanged
    snap2 = er.snapshot()
    assert snap2["registered"] == ["u1"]
    assert snap2["waitlist"] == ["u2"]
```

### Run

```bash
pytest tests.py -v
```

If you want, I can also add **two “grader-style” stress tests** that catch weak solutions:

1. repeated cancel + re-register cycles
2. long FIFO waitlist with removals from middle and promotions in between
}
--------------------------------------------------


Prompt #5: {sure do that}
Response #5: {Absolutely — here are **two “grader-style” stress tests** that catch a lot of weak implementations. Paste them at the bottom of your `tests.py`.

They focus on:

1. **repeat cancel + re-register cycles** (state consistency + duplicates + promotion stability)
2. **long FIFO waitlist with middle removals + promotions** (FIFO correctness under churn)

```python
def test_stress_repeated_cancel_and_reregister_cycles():
    # Validates: consistent state after many operations, re-register allowed after cancel,
    # no duplicates, deterministic promotion behavior.
    er = EventRegistration(capacity=2)

    # Fill capacity + waitlist
    er.register("u1")
    er.register("u2")
    er.register("u3")
    er.register("u4")

    # Cycle: cancel a registered user, ensure FIFO promotion, then re-register canceled user
    er.cancel("u1")  # promotes u3
    assert er.snapshot()["registered"] == ["u2", "u3"]
    assert er.snapshot()["waitlist"] == ["u4"]

    er.register("u1")  # should go to waitlist (since capacity full)
    assert er.status("u1") == UserStatus("waitlisted", 2)
    assert er.snapshot()["waitlist"] == ["u4", "u1"]

    er.cancel("u2")  # promotes u4
    assert er.snapshot()["registered"] == ["u3", "u4"]
    assert er.snapshot()["waitlist"] == ["u1"]
    assert er.status("u2") == UserStatus("none")

    # Now re-register u2; should go to waitlist (capacity full)
    er.register("u2")
    assert er.snapshot()["waitlist"] == ["u1", "u2"]

    # Cancel u3 -> promote u1
    er.cancel("u3")
    assert er.snapshot()["registered"] == ["u4", "u1"]
    assert er.snapshot()["waitlist"] == ["u2"]

    # Cancel u4 -> promote u2
    er.cancel("u4")
    assert er.snapshot()["registered"] == ["u1", "u2"]
    assert er.snapshot()["waitlist"] == []

    # Ensure no duplicates and status consistent at end
    assert er.status("u1") == UserStatus("registered")
    assert er.status("u2") == UserStatus("registered")
    assert er.status("u3") == UserStatus("none")
    assert er.status("u4") == UserStatus("none")


def test_stress_long_waitlist_middle_removals_and_fifo_promotions():
    # Validates: FIFO ordering under churn + correct position updates after removals/promotions
    er = EventRegistration(capacity=3)

    # Register 3, then waitlist 6
    er.register("r1")
    er.register("r2")
    er.register("r3")

    er.register("w1")
    er.register("w2")
    er.register("w3")
    er.register("w4")
    er.register("w5")
    er.register("w6")

    assert er.snapshot()["registered"] == ["r1", "r2", "r3"]
    assert er.snapshot()["waitlist"] == ["w1", "w2", "w3", "w4", "w5", "w6"]

    # Remove from middle of waitlist
    er.cancel("w3")
    assert er.snapshot()["waitlist"] == ["w1", "w2", "w4", "w5", "w6"]
    assert er.status("w4") == UserStatus("waitlisted", 3)

    # Cancel a registered user -> promote w1 (FIFO)
    er.cancel("r2")
    assert er.snapshot()["registered"] == ["r1", "r3", "w1"]
    assert er.snapshot()["waitlist"] == ["w2", "w4", "w5", "w6"]

    # Cancel another registered user -> promote w2
    er.cancel("r1")
    assert er.snapshot()["registered"] == ["r3", "w1", "w2"]
    assert er.snapshot()["waitlist"] == ["w4", "w5", "w6"]

    # Remove head of waitlist (w4) then cancel registered to promote next (w5)
    er.cancel("w4")
    assert er.snapshot()["waitlist"] == ["w5", "w6"]
    assert er.status("w5") == UserStatus("waitlisted", 1)

    er.cancel("r3")  # promote w5
    assert er.snapshot()["registered"] == ["w1", "w2", "w5"]
    assert er.snapshot()["waitlist"] == ["w6"]
    assert er.status("w6") == UserStatus("waitlisted", 1)

    # Final sanity: no removed users remain
    assert er.status("r1") == UserStatus("none")
    assert er.status("r2") == UserStatus("none")
    assert er.status("w3") == UserStatus("none")
    assert er.status("w4") == UserStatus("none")
```

### Run them

```bash
pytest tests.py -v
```

If any of these fail, it’s usually because the implementation:

* doesn’t update waitlist positions correctly after removals/promotions
* breaks FIFO ordering under churn
* accidentally allows duplicates during re-register cycles
}
--------------------------------------------------


Prompt #5: {}
Response #5: {}
--------------------------------------------------